type User {
  id: ID! @unique
  name: String!
  privileges: [Privilege!]!
  actionTypes: [String!]!
}

type Privilege {
  id: ID! @unique
  name: String! 
  users: [User!]!
  actionTypes: [String!]!
  privileges: [Privilege!]!
}

